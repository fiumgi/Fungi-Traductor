# Fungi Traductor — dependencias
# Instalar con:  pip install -r requirements.txt

# ── Núcleo ────────────────────────────────────────────────────────────────────
argostranslate>=1.9.0   # motor de traducción offline

# ── Opcionales (mejoran la experiencia) ──────────────────────────────────────
langdetect>=1.0.9       # detección automática de idioma  (pip install langdetect)
pyttsx3>=2.90           # texto a voz offline             (pip install pyttsx3)

# ── Solo para empaquetar como .exe ────────────────────────────────────────────
pyinstaller>=6.0
